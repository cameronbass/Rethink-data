<% if user_signed_in? %>

  <%= link_to "Sign out", destroy_user_session_path, :method => :delete, class: "button", id: "sign-out" %>

<% else %>

  <%= link_to "Sign In", new_user_session_path, class: "button" %>

<% end %>
